node_modules/
.next/
out/
build/
.env
.env.local
.env.*.local
.data/
# Runtime document storage (local driver). Anchored to the repo root so it does
# not accidentally ignore source directories such as src/lib/storage.
/storage/
*.tsbuildinfo
next-env.d.ts
.DS_Store
coverage/
playwright-report/
test-results/
